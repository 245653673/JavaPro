package rpc.first.serialize.support.hessian;

import rpc.first.serialize.support.MessageCodecUtil;
import rpc.first.serialize.support.MessageDecoder;

public class HessianDecoder extends MessageDecoder {

    public HessianDecoder(MessageCodecUtil util) {
        super(util);
    }
}